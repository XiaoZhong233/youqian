<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gz.scau.zhonghaowei.xiaoshoukuaisuan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.Activity.MainActivity"
            android:windowSoftInputMode="stateHidden|adjustPan" />
        <activity android:name=".controller.Activity.RecordListActivity" />
        <activity android:name=".controller.Activity.ViewPagerRecordActivity" />
        <activity android:name=".controller.Activity.RecordActivity" />
        <activity
            android:name=".controller.Activity.EntryActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.Activity.AddRecordActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".controller.Activity.RecordsDetailActivity" />
        <activity android:name=".controller.Activity.AddBudgetActivity" />
        <activity android:name=".controller.Activity.QueryActivity" />
        <activity android:name=".controller.Activity.QueryResultActivity"></activity>
    </application>

</manifest>